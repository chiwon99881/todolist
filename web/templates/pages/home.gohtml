<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List | Golang</title>
  </head>
  <body>
  <div class="container">
    <header>
      <div>
        <span>To Do List</span>
      </div>
      <div>
        <span class="title">치원이의 Golang ToDoList</span>
      </div>
      <div>
        <a href="/add">Add To Do</a>
      </div>
    </header>
    <div class="main">
      <div class="todo-container">
        {{range .ToDos}}
          <div class="todo-card">
            <div class="todo-info">
              <span class="todo-id">{{.ID}}.</span>
              <span class="todo-caption">{{.Caption}}</span>
            </div>
            <form method="POST" action="/action" class="todo-check">
              {{if .Excute}}
                <button class="todo-excuted" />
              {{else}}
                <button class="todo-nonexcuted" />
              {{end}}
            </form>  
          </div>
        {{end}}
      </div>
    </div>
  </div>
  </body>
</html>

<style> 

  body {
    padding: 0 70px;
  }
  a {
    text-decoration: none;
  }
  .container {
    position: relative;
    width: 100%;
    height: 100%;
  }
  header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    max-width:100%;
    height: 70px;
    border-bottom: 1px solid #DDDDDD;
    max-height: 50px;
    background-color: white;
  }
  .title {
    font-weight: 600;
    font-size: 30px;
  }
  .main {
    margin-top: 20px;
    box-sizing: border-box; 
    width: 100%;
    height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #DDDDDD;
  }
  .todo-container {
    box-sizing: border-box;
    width:80%;
    height:100%;
    display: flex;
    padding-top: 40px;
    justify-content: center;
  }
  .todo-card {
    width:30%;
    height:50px;
    display:flex;
    align-items:center;
  }
  .todo-info {
    width:80%;
    display:flex;
    flex-wrap: wrap;
    border-bottom: 0.5px solid #DDDDDD;
  }
  .todo-id {
    font-size: 20px;
  }
  .todo-caption {
    font-size: 20px;
  }
  .todo-check {
    width:20%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .todo-excuted {
    padding: 10px;
    background-color: red;
    border: 0.5px solid #DDDDDD;
    cursor: pointer;
  }
  .todo-nonexcuted {
    padding: 10px;
    background-color: white;
    border: 0.5px solid #DDDDDD;
    cursor: pointer;
  }

</style>